[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@10.33.2",
  "pnpm install --frozen-lockfile --ignore-scripts"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
